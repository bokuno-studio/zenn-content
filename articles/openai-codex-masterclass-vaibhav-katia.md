---
title: "OpenAI Codexマスタークラス: プラグイン・サブエージェント・Hooks を62分で完全解説【AI Engineer Europe 2026】"
emoji: "🤖"
type: "tech"
topics: ["openai", "codex", "aiagent", "mcp", "githubactions"]
published: false
---

:::message
この記事は以下の YouTube 動画（約62分）を日本語でまとめたものです。
登壇者: Katia Gil Guzman（@kagigz）・Vaibhav (VB) Srivastav（@reach_vb）/ OpenAI Developer Experience チーム
イベント: AI Engineer Europe 2026（London）
:::

https://www.youtube.com/watch?v=MhHEGMFCEB0

## はじめに

「Codex はコーディングツールではない」——OpenAI の開発体験チームが AI Engineer Europe 2026（London）でそう言い切り、プラグイン・サブエージェント・Hooks・コードレビューまでを62分でデモしたワークショップです。

週間アクティブユーザーが1月から3倍に増えて300万人を超えた Codex の「正しい使い方」が凝縮されています。

---

## Codex とは何か

![AI Engineer Europe 2026 オープニング](/images/openai-codex-masterclass-vaibhav-katia-00-00-30.jpg)

Codex は **オープンエンドなソフトウェアエンジニアリングエージェント**です。コードを書くだけでなく、コマンドの実行・テスト・コードベース探索・PR レビューまで、ソフトウェアエンジニアがやること全般をこなします。

| レイヤー | 内容 |
|---------|------|
| **Foundation models** | GPT-5.2 → 5.3 → 5.4（+ mini / nano）系列 |
| **Unified Agent Harness** | ツール実行・環境セットアップ・安全機能を束ねるラッパー |
| **Surfaces** | App・IDE拡張・CLI・Slack・GitHub |
| **Integrations** | Figma・Linear・Notion などと接続可能 |

モデルは GPT-5.4 で WebSocket 導入により 1.75x 高速化、Fast Mode でさらに +2x が使えます。

---

## 最近リリースされた主な機能

![What's New スライド](/images/openai-codex-masterclass-vaibhav-katia-00-11-30.jpg)

| 機能 | 概要 |
|------|------|
| **Plugins** | Skills + Apps + MCP サーバーを1つにバンドルして再利用 |
| **Mini / Nano モデル** | サブエージェント向けの高速・低コストモデル |
| **Subagents** | タスクを並列・独立したエージェントに分割して実行 |
| **Automations** | バックグラウンドで定期実行するスケジュールタスク |
| **/review** | GitHub PR・CLI・Cursor から使えるコードレビュー |
| **Codex Security** | コミット単位で脆弱性を検出・パッチを自動生成 |

---

## Plugins: スキル・App・MCP をまとめて再利用

プラグインは **Skills（再利用可能な指示）+ Apps（外部サービス接続）+ MCP サーバー** を1つのバンドルにしたものです。毎回手動でセットアップしなくてよくなります。

### デモ1: Game Studio プラグインでゲームを自動生成

![Game Studio プラグイン + Image Gen + Playwright Interactive](/images/openai-codex-masterclass-vaibhav-katia-00-17-00.jpg)

```
「Game Studio プラグインを使って、ブロックのプラットフォームを飛び越える2Dプラットフォーマーゲームを作って」
```

これだけで Codex が:
1. **Image Gen** でスプライト（キャラクター・背景）を自動生成
2. **Playwright Interactive** でブラウザ上に表示・動作確認
3. バグを検出して自動修正

指示は一言で、スプライトも含めて完成したゲームが生成されました。

### デモ2: Google Drive プラグインでスプレッドシートを自動更新

```
「コードベースにある Codex イベント一覧を Google Drive のスプレッドシートに書き込んで」
```

Codex が YAML ファイルを解析 → 57件のイベントをスプレッドシートに書き込み（約2分で完了）。

### Automations: バックグラウンドで定期実行

Katia が実際に使っている自動化の例：

- **毎朝 Slack チェック**: 返信が必要なメッセージをフラグ、前日の要約をトピック別に整理
- **Gmail フィルタ**: 返信が必要なメールだけを抽出、時間的緊急度・スパム判定も

「これで1日に何時間も節約できている」とのこと。

---

## コードレビュー: OpenAI 全 PR を Codex が自動レビュー

![コードレビューのデモ](/images/openai-codex-masterclass-vaibhav-katia-00-28-00.jpg)

> **OpenAI の全従業員（Greg 含む）の全 PR が、デフォルトで Codex にコードレビューされています。**

使える場所:

| 場所 | 方法 |
|------|------|
| **GitHub** | ChatGPT アカウントを連携 → PR 作成時に自動レビュー |
| **CLI / App** | `/review` コマンド |
| **Cursor** | Cursor プラグイン（VB と Dom が先週リリース） |

レビューは差分だけでなく**リポジトリ全体のコンテキスト**を参照するため、変更していない別モジュールへの二次的影響まで指摘します。

---

## サブエージェント: タスクを並列化する

![openai/codex-agents リポジトリのサブエージェントペルソナ一覧](/images/openai-codex-masterclass-vaibhav-katia-00-35-30.jpg)

サブエージェントは**マスタータスクを分割して複数のエージェントに並列実行させる**仕組みです。

### デフォルトで用意される3ペルソナ

| ペルソナ | 用途 |
|---------|------|
| **default** | 汎用フォールバック |
| **worker** | 実装・機能開発に特化 |
| **explorer** | コードベース探索・分析に特化 |

### カスタムサブエージェントの作り方

TOML ファイルを1つ書くだけです（[openai/codex-agents](https://github.com/openai/codex-agents) に45件以上のサンプルあり）。

```toml
name = "architecture-critic"
description = "提案されたアーキテクチャをトレードオフの観点でレビューする"
nickname_candidates = ["Architecture Critic"]
sandbox_mode = "read-only"
model_reasoning_effort = "high"
developer_instructions = """
トレードオフ優先でアーキテクチャをレビューする。
不必要な抽象化・密結合・マイグレーションコスト・運用複雑性を優先的に指摘する。
"""
```

設定できるもの:
- `model`: 使用するモデル（mini / nano も指定可）
- `sandbox_mode`: `read-only` / `full-access`
- `model_reasoning_effort`: `low` / `medium` / `high`
- MCP サーバーの追加（Sentry・Linear など）
- Skills の追加

### デモ: 20サブエージェントが45ペルソナを並列レビュー

Codex が複雑タスクと判断すると**自動でプランモード**を起動し、タスクを分割。20エージェントが同時に走り、全ペルソナファイルをレビュー。「investigate-reporter は権限過多（P1）」「writer はサンドボックスの設定ミスマッチ」などの問題を検出しました。

---

## Bleeding Edge: 実験的な機能

![Hooks の設定例](/images/openai-codex-masterclass-vaibhav-katia-00-51-00.jpg)

### Gorge & Approvals（実験的）

`/experimental` で有効化できます。Codex が特権的な操作（ディレクトリ削除・サーバー公開など）を実行しようとするとき、**サブエージェントが自動で安全性を判断**して承認します。人間の承認疲れを減らすことが目的です。

### Hooks（実験的）

3種類のフックが使えます:

| フック | タイミング |
|--------|---------|
| `pre-tool-use` | ツール実行前 |
| `start` | セッション開始時 |
| `stop` | セッション終了時 |

VB が実際に使っている stop hook:

```python
# keep_going.py
# セッション終了のたびに「もう1パス続けて、バリデーションを1つ実行して止まれ」と指示する
# 長時間タスクをセルフで継続させるための仕組み
```

### Codex Security

GitHub リポジトリをコミット単位でスキャンして脆弱性を検出し、パッチ PR を自動作成します。

### Personality Changes

`/footics` の Personalization からカスタム人格（フレンドリー・実用的など）と追加指示を設定できます。

---

## Q&A: クラウド実行とスキル

**Q: ローカル作業中のタスクをクラウドに引き継げる？**

App の「Cloud」を選択すると可能。**Best of N**（例: 4並列で実行して最良を選ぶ）機能もあります。

**Q: クラウドモードでもローカルのスキルは使える？**

ローカルスキル（Python スクリプトを含むもの）はセキュリティ上の理由で実行されません。ただしリポジトリにチェックインされたスキルはコードベースの一部として読めます。改善は継続中とのこと。

---

## まとめ

| ポイント | 内容 |
|---------|------|
| Codex はコーディングツールではない | テスト・レビュー・セキュリティ・ゲーム生成まで対応 |
| プラグインで繰り返し作業をゼロに | Skills + Apps + MCP を1バンドルで再利用 |
| コードレビューは OpenAI 全社標準 | Greg の PR も Codex がレビューしている |
| サブエージェントで並列化 | TOML 1ファイルでカスタムペルソナを定義 |
| Hooks で長時間タスクを自律実行 | stop hook でセルフ継続が可能 |

openai/codex-agents リポジトリには accessibility-reviewer・architect・bug-reproducer・security-reviewer など45件以上のペルソナが公開されており、すぐに使えます。

---

## 📝 動画全文書き起こし（日本語訳）

**[0:00]**
皆さん、ありがとうございます。今日は Codex についてお話しします。私は Katia（Katia Gil Guzman）、こちらは VB（Vaibhav Srivastav）です。私たちは OpenAI のデベロッパーエクスペリエンスチームで、ロンドンを拠点に活動しています。私たちの役割は、開発者が Codex を含む OpenAI の製品を最大限に活用できるよう支援することです。

今日の構成は、まず Codex の概要を簡単に説明し（どれくらいの方が Codex を使っているか挙手いただけますか？ありがとうございます）、その後デモをお見せします——プラグイン・オートメーション、そして VB がサブエージェントと最先端機能を紹介します。すでに Codex を使っている方でも、知らなかったことを学んでいただけるはず。最後に Q&A の時間も設けます。ワークショップ形式なので、差し迫った質問があればいつでも聞いてください。ラップトップをお持ちの方は一緒に試してみてください。

まず Codex の概要から。Codex はオープンエンドなソフトウェアエンジニアリングエージェントです。単なるコーディングエージェントではなく、コマンド実行・テスト・コードベース探索など、ソフトウェアエンジニアがやること全般をこなせます。モデルが基盤にあります——例えば GPT-5.2、GPT-5.2 Codex が以前のバージョン。超高速モデルの Spark バージョン、現在の最先端モデルは GPT-5.4 で、先週 mini バージョンも登場しました。モデルが改善されるたびに Codex もその恩恵を受けます。

モデルの上には「ユニファイドエージェントハーネス」と呼ぶものがあります。エージェントの動作を評価・管理し、ツール実行・環境セットアップを担うラッパーで、安全性もここに組み込まれています。これ全体が Codex です。

利用できるサーフェスは複数あります——Codex App（後で詳しく説明）、IDE 拡張機能、CLI、そして Slack（OpenAI 社内では Slack に Codex をピン留めして修正を依頼しています）や GitHub。さらに Figma・Linear・Notion などのツールと統合して、ソフトウェアエンジニアの同僚がするすべてのことを Codex にさせられます。

では VB に、モデルについてもう少し詳しく話してもらいます。

**VB:** おはようございます。Codex App・IDE 拡張・CLI などのハーネスも、それを動かすモデルがなければほとんど意味がありません。少し振り返ると、私が OpenAI に入ったのは12月で、当時のモデルは GPT-5.2 でした。そこから GPT-5.2 Codex（長時間タスク・複雑なタスクにモデルを特化させたもの）をリリースし、GPT-5.3 Codex、さらに Cerebras との連携で GPT-5.3 Codex Fast、そして最近 GPT-5.4 をリリースしました。このモデルとハーネスのフライホイールをできるだけ速く回し、最前線をできるだけ速くお届けしようとしています。

**[5:00]**
**VB:** 補足すると、大きなモデル（長時間・複雑なタスク向け）を推し進めると同時に、GPT-5.4 mini と GPT-5.4 nano もリリースしました——短時間タスクやサブエージェント向けです（後ほど説明します）。

もう一つ強調したいのはスピードの改善です。モデルを良くするだけでなく、できるだけ速くお届けすることにも取り組んでいます。具体的には WebSocket を導入しました——デバイスと API の間に接続を作ることで、コストをかけずに約 1.75 倍速いトークンを提供できます。さらに Fast Mode を使うと 2 倍の追加速度が得られます。これは継続的に改善しており、さらなるスピード改善が来ます。

年初に Codex App を立ち上げました（使ったことがある方は？かなりいますね）。正直、私自身12月まで熱烈な CLI ユーザーでしたが、App のテスト中にメインのワークフローになりました。理由はプロジェクトをまたいだ作業がしやすいこと、そして同じプロジェクト内で複数フィーチャーを同時並行で開発できること。ワークツリーを使えば、個々のフィーチャーリクエストやバグ修正・Q&A をそれぞれ独立したワークツリーで同時に進められます——コンテキストスイッチなしに。ネイティブワークツリーサポートを提供していることを誇りに思っています。

オートメーションサポートも改善しました——毎日9時に実行するプロセスや、カレンダーを見てブリーフィングを作るなど、バックグラウンドで実行できます。ワークツリーとネイティブ Git サポートにより、プロジェクトをまたいで自由に push できます。

Windows サポートも年初に追加しました（最初は macOS のみ）。ネイティブ Windows サンドボックス付き——Windows でこれを実現している他のエージェントハーネスはありません。Windows をお使いの方はいますか？1名いますね、良かった。

**[10:00]**
**VB:** では Codex App とモデルの話はここまでにして、最近リリースした機能について。降順で並んでいます。最近リリースしたのはプラグイン——Skills・MCP・プロンプトを一つにまとめてバンドルし、ビルド中により細やかなマッチングを可能にします。ミニモデルもリリースしました——サブエージェントとうまく連携して、特定のフィーチャー・バグ修正・Q&A を並列化できます。コードレビュー、セキュリティなど他にも多くの機能を紹介します。

なぜこれを強調するかというと、OpenAI はコミュニティが Codex を受け入れてくれていることをとても嬉しく思っているから。昨夜、週間アクティブユーザー300万人を突破しました。これは大きなマイルストーンです。エンタープライズや多くのスタートアップが Codex を構築に使い始めています。質問はいつでもどうぞ。

**Katia:** 300万週間アクティブユーザーは本当にすごい。しかも1月から3倍以上。数ヶ月でこれほどの採用を見られてとても嬉しい。ではプラグインの話をしましょう。

プラグインは Codex の比較的新しい機能です。Skills・Apps・インテグレーション・MCP サーバーを一つにまとめて再利用可能なワークフローにバンドルします。

**Skills（スキル）** は特定のプロセス向けにパッケージされた再利用可能な指示です。よくやることがあれば、スキルを作れば Codex がそれを把握します。指示・スクリプト・リソースを与えられ、繰り返しの必要がなくなります。繰り返しのあるメタワークフローはスキルにパッケージできます——Codex 自身にスキルを作ってもらうこともできます。

**Apps（アプリ）** は他のサービスへの接続です。毎日使うツール——Notion・Linear などに Codex を接続できます。

**MCP サーバー** はご存知かもしれませんが、Codex の能力を外部システムのツールで拡張します。

これら三つはそれぞれ単体でも便利ですが、プラグインとしてバンドルすることで手動設定が不要になります。複数のスキルをインストールしたり、複数の接続を設定したり、複数の MCP サーバーを接続したりする必要がなく、プラグインを一つ追加するだけ。

もう一つ話したいのがオートメーション——私の個人的なお気に入りです。バックグラウンドで実行できる、cron ジョブのようなものを設定できます。アプリを接続してプラグインも使えて、スケジュール実行できます。例えば毎日特定の時間に実行する指示を設定するだけ。デモでお見せします。

ウェブ・アプリ・ゲーム開発向けの特定スキルも紹介します。多くの開発者からアプリやゲームを Codex で作りたいという声を聞き、毎回同じスキルを繰り返し使っていたので、特定のプラグインにパッケージしました。特に強力な2つのスキルを紹介します——**Playwright Interactive**（ヘッドレスブラウザ・サンドボックスブラウザで、クリックやナビゲート・スクリーンショット取得が可能）と **Image Gen**（アプリやゲームのビジュアルアセットを生成）。

**[15:00]**
**Katia:** デモを始めます。まずこのタスクを先に実行しておきます（昨日は1時間かかったので）。Game Studio プラグインを使っています——ゲーム開発に役立つスキルのバンドル。Image Gen でビジュアルアセット（スプライト）を作り、Playwright で動作確認・デバッグします。プロンプトは「2Dプラットフォーマーゲームを作って」だけ。これを動かしておいて、プラグインの説明を続けます。

別のプロジェクト——開発者サイトのリポジトリに切り替えます。私たちの開発者サイトには Codex と AI の Tip が大量に掲載されています。それらすべてが YAML ファイルとしてコードベースに入っています。Google Drive プラグインを追加しました——Codex が私の Google Drive にアクセスできます。「Codex Events」というスプレッドシートを用意しておきました（イベント名・日付・都市）。コードベースに掲載されている最新の Codex ミートアップでこのシートを更新してもらいます。

（タスク実行中）ゲームタスクを見てみましょう——かなりかっこいいスプライトを生成しています。後で見てみましょう。

オートメーションについて。インタラクティブに使うのではなく、バックグラウンドで実行するように設定します。例えば私が設定していて本当に助かっているのが Slack メッセージ用。Slack に Codex を接続して、「毎日この時間に、返信すべきメッセージと時間的に重要かどうかをチェックして。昨日 Slack で何が起きたかも要約して、トピックごとに整理して」とお願いしています。重要な社内情報が流れるチャンネルがあるので、そこも見落とさないように。

もう一つは Gmail 連携——本当に大量のメールが届くので、実際に返信すべきメールだけを抽出してもらっています。時間的に重要か・正当なリクエストかもチェック。これで1日何時間も節約できています。

オートメーションの作り方は、メニューから作る方法と、「Slack で Codex のユースケースについて言及しているものを見て、ウェブサイトに載せるべき重要なユースケースをリストにしてくれる自動化を作って」と話しかける方法があります。

（スプレッドシートタスクを確認）2分かけてコードベースを分析し、YAML ファイルで Codex イベントのソースを見つけ、57件のイベントを書き込みました。スプレッドシートが更新されています。これはシンプルな例ですが、データレビューなど何でもできます——コードベース全体へのアクセスがあり、他の CSV ファイルも入力として渡せます。

ゲームを確認しましょう——Image Gen でスプライトを生成しています。結果を見てみましょう（昨日完成したバージョン）。これは一切手を加えていません。Codex だけで作ったものです。「レンガのプラットフォームがある2Dプラットフォーマーゲームを作って」それだけが私の指示。全体の UI はさらに改善できますが、プラットフォーマーゲームとしてはかなりいい。キャラクターのスプライトは最低5種類——自分では何もしていない。自分の顔を入力にして、Image Gen でカスタムゲームを作ることもできます。これが Game Studio プラグインです。Web アプリ向けにも同様のものがあります。これらのスキルをすべてバンドルしたものです。

ではVBにお渡しします。

**[25:00]**
**VB:** Katia、ありがとう。ここまでのチェックポイントとして——モデル・Codex エコシステム・利用できるサーフェス・プラグインの使い方・プラグインの種類を見ました。Plugin Creator で独自プラグインも作れます。オートメーションと Image Gen も見ました。

これほどエージェントに仕事を委任していくと、エージェントが生成するものの品質を確保したい——複数のフィーチャーを同時に進めていると、毎行コードを見ていくのは不可能。少なくとも最初のパスで信頼できるコードレビューが必要。Codex コードレビューは業界最高レベルの一つだと思っています（自分のことを褒めているとは分かりつつ）。Discord や他のプラットフォームでも絶賛されています。

まずサーフェスを説明します。**GitHub** では ChatGPT アカウントを GitHub に接続し、すべてのプルリクエストを自動的に Codex にレビューさせられます——P0・P1・P2 形式で「これが欠けている」「このバグを直してください」などのコメントがプルリクエストに付きます。Codex CLI や Codex App では `/review` コマンドで大規模なレビュープロセスを起動できます。先週は同僚の Dom と **Cursor プラグイン**もリリースしました——Cursor セッション内から Codex を呼び出して同じ最高品質のコードレビューができます。

実際の私のワークスペースで見てみましょう（これは本当に私の日常の作業環境です——見てはいけないものが映っていたら目を閉じてください）。フィーチャーリクエストに対して変更を加えたとします。「変更をレビューして」と聞くと、ベースブランチを選ぶオプションが出てきます。今回はコミットされていない変更をレビューさせます。

すると全く新しいスレッドが立ち上がります。そのスレッドで Codex は、差分だけでなくリポジトリ全体のコンテキストを参照しながらレビューします。多くの場合、プルリクエストで触れていないモジュールへの**二次的影響**を検出します——これが非常に効果的で、OpenAI 全従業員の全 PR（Greg のものも含め）がデフォルトで Codex コードレビューを受けます。

P1・P2 形式でアップデートが出てきます。その後 Codex に「このパスで修正して」や「この変更に基づいて PR を開いて」と依頼できます。

**[30:00]**
**VB:** 次はサブエージェント——私が個人的に興奮している機能です。サブエージェントは、マスタータスクを分解可能・並列・独立したタスクに分け、それぞれのエージェントに割り当て、各エージェントが独立して動いて最終的に結果を返す仕組みです。API キーやサブスクリプションの範囲内でいくつでも起動できます。

サブエージェントでできることの例として、画面左のスクリーンショットは Codex Agents リポジトリ（まだ公開していませんが近々公開予定）——多くのサブエージェントペルソナが含まれています。スペックを変更したいとき、40〜50個のペルソナファイルをすべてレビューして最新スペックに合っているか確認させます。サブエージェントなしだと1ファイルずつ順番にレビューしますが、サブエージェントを使うと「レビュースライス」を作って、20個のサブエージェントに分割して並列処理させます。

デモを見てみましょう。このリポジトリには多数のペルソナがあります（accessibility reviewer・architect など）——自分で作ることもできますし、後で説明します。各ペルソナには名前・説明・サンドボックスモード（書き込みのみ・読み取りのみ）・指示があります。

「全サブエージェントをレビューするために20のサブエージェントを立ち上げて」と指示します。シンプルなタスクです。すると Codex が「45個のペルソナファイルがある」と検出し、20個のレビュアーを作ってレビューを割り当てます。

興味深いのは二つ。一つは Codex が「これは複雑なタスクかもしれない」と自動的にプランモードを起動したこと——5つのタスクに分解するプランを立てました（明示的に起動もできますが、自動で判断しました）。そして20個のサブエージェントを起動します（私の環境では同時6スレッドの制限があるので6つ並列ですが、制限解除も可能）。各エージェントの動作をリアルタイムで確認できます。メインの Codex モデルが各サブエージェントに正確なファイルと、repo.md・contributing.md にあるリポジトリガイダンスも伝えていることがわかります。

**[35:00]**
**VB:** このリポジトリが対象のリポジトリです——codex-agents リポジトリにペルソナが多数あります。TOML ファイルを自分で作ることもできますし、Codex に作ってもらうこともできます。各ペルソナの構成は：名前・説明・サンドボックスモード（読み取り専用か書き込みも可能か）・指示など。

（デモ実行中）サブエージェントが全ペルソナファイルを確認し、他のサブエージェントを起動してレビューしています。プランモードで5つのタスクに分解され、20個のレビュアーが45個のペルソナファイルを処理しています。各エージェントが独立して動いた後、最終的にすべてのフィードバックを統合します。

（結果確認）「investigator は過剰権限（P1）」「verify はサンドボックス設定ミスマッチ」「writer も同様」——これは非常に有用で、順次処理するよりはるかに速い。

**[40:00]**
**VB:** サブエージェントの応用例は他にもあります。例えばサイバーセキュリティの観点から、特定のコミットやリポジトリに対して複数の視点で脆弱性分析を行い、脆弱性マップを作ることができます。私がよく使うのはフィーチャー設計のブレインストーミング——「この機能を実装する5〜6〜10通りの方法を考えて」と Codex に複数の計画を立てさせ、気になるアプローチをさらに複数のサブエージェントで掘り下げます。

デフォルトでは3種類のサブエージェントペルソナが提供されています：
1. **Default**：汎用フォールバックエージェント
2. **Worker**：実行フォーカス。フィーチャーリクエストの実装などに使う
3. **Explorer**：今見たもの。探索・調査に使う

各ペルソナでモデル・推論努力・サンドボックスモードを設定できます。レビューエージェントは100%読み取り専用にすべき——決して実行させない。セキュリティ脆弱性検出も読み取り専用で。ドキュメント作成エージェントは書き込みアクセスも必要。

各サブエージェントにさらに能力を与えることもできます——MCP アクセスを付与して、例えば Sentry（バグレポートへのアクセス）や Linear（バックログへのアクセス）を与える。スキルを付与することも可能。かなり細かくカスタマイズできます。

（先ほどの結果確認）investigator が過剰権限・verify がサンドボックスミスマッチ・writer も同様——順次処理や個別確認より大幅に時間が節約できます。

**[45:00]**
**VB:** カスタムサブエージェントについて。デフォルトの3ペルソナに加えて、自分でサブエージェントを作れます。過去のセッションを見て Codex にサブエージェントを提案させることもできます。両方の方法がうまく機能します。

例として PR Explorer サブエージェントがあります——コードベースを読んで GPT-5.3 Codex Fast（高速な研究プレビューモデル）を使い、読み取り専用サンドボックスで動作します。「探索モードに留まれ、実行パスをトレースせよ、修正を提案するな、検索だけして何をすべきか把握せよ」という指示。

今すぐ Docs Researcher サブエージェントを作ってみましょう。「このペルソナで Docs Researcher サブエージェントを作って」と依頼するだけ。Codex は仕組みを理解しているので、TOML ファイルを自動生成します。このケースでは DS チームが作った Docs MCP サーバー——API リファレンス・ドキュメント・ガイド・クイックスタートなどすべてをパッケージしたもの——を MCP サーバーとして追加します。こうすることで「GPT-5.4 と WebSocket の最良の使い方は？」「GPT リアルタイム API を使った React プラグインを作るには？」と聞くとすべてのドキュメントを参照できるエージェントが完成します。

サブエージェントの活用例：長いビルドプロセスやテストがある場合、ローカルでテストを実行するサブエージェント、最新の Git を pull するサブエージェント、Linear のイシューからコンテキストを取得するサブエージェントなど。Codex に過去のセッションを見てもらって「どんなオートメーションやサブエージェントを使えばいいか提案して」と聞くのがおすすめです。

**[50:00]**
**VB:** ここからは最先端の機能です。あまり大々的に発表していなかったものをご紹介します。

**Gorge & Approvals（実験的機能）** は `/experimental` で有効化できます。Codex App で「experimental」を確認すると有効化できます。

誰でも一度は `--dangerously skip permissions` を使って Codex にやりたいことを何でもさせたことがあると思います——これは安全ではありません。Gorge & Approvals はこのために作りました。Codex が特権操作が必要なとき（「このディレクトリを削除していいか？」「このサーバーを起動して特定のファイルをインターネットに公開していいか？」など）、別のサブエージェントを起動してそれが人間の介入が必要かどうかを判断します。ほとんどの場合は介入不要なのでそのまま実行します。これによってエージェントに「これをやっていい？あれをやっていい？」と毎回承認を求められる疲労を減らすことが目的です。

**Hooks（実験的機能）** も継続的に改善しています。現在3種類のフックをサポートしています：
- **start**（セッション開始時）
- **pre-tool-use**（各ツール使用前）
- **stop**（セッション終了時）

Hooks でできること：セッション開始時に最新の GitHub リポジトリを pull する、各ツール使用ごとに Codex がやったことをドキュメント化する、long running タスクのために stop フックで「もう一周続けて」と指示する（私が個人的によく使います）。

実装例：stop フックに Python スクリプト（keep_going.py）を登録しておくと、stop フックが発生するたびに「もう一周。一つ検証コマンドを実行して、もう一つ改善してから止めて結果を教えて」と指示します。本当に長いタスクは設定して放置しておくと、自律的に続けてくれます。

**[55:00]**
**VB:** **Personality Changes（パーソナリティ変更）**——Codex の人格をカスタマイズできます。よりフレンドリーな人格や、実用的な人格など。カスタム指示も追加できます（例：常に何をしているかを引用して）。

**Codex Security**——GitHub プロジェクトの脆弱性を見つけて修正する最先端機能。コミットごとに脆弱性パッチを作り、Codex でその変更もパッチします。

**Cursor プラグイン**——先ほど言及した通り。Cursor コードセッション内から Codex を呼び出して、同様のコードレビューをリクエストできます——「今まで変更したものをレビューして」や「今まで変更したものに対して adversarial レビューをして」など。

以上です。ご参加ありがとうございました。ご質問があればどうぞ。あまり Q&A の時間がありませんが、数問受け付けます。セッション後も残っていますので、後でも質問できます。

**[60:00]**
**Q:** セッションデータはどこに保存されていて、過去のセッションを参照するにはどうすればいいですか？

**VB:** `.codex` フォルダ内の `.sessions` に格納されています。Codex App・Codex Alive などどれでも「セッションを見て、○○して」と聞けばスキャンして参照できます。

**Q:** クラウドエージェントにタスクを引き継ぐことはできますか？ローカルで作業中に、ラップトップを閉じたいんですが。

**Katia:** はい、できます。触れませんでしたが、Codex App から直接できます。「Cloud」を選択して、並列で何回実行するかも指定できます——「Best of N」機能で4回並列実行して最良の出力を選ぶことも可能。Codex App・IDE 拡張・ウェブから直接アクセスできます。この機能はさらに改善が続く予定です。

**Q:** クラウド実行ではサブエージェントがサポートされていないと思いますが、リポジトリにあるスキルがクラウドで使われないのでは？今後対応予定はありますか？

**VB:** ロードマップを全部話すとあれですが、ローカルスキルをクラウドで実行することは安全上の理由から現時点では制限しています——スキルが信頼できるかどうかシステムが判断できないため（Python スクリプトなどを実行するスキルは動かしません）。ただしリポジトリにチェックインされたスキルは参照できます——コードベースにあるので読み取りはできます。ローカルのものほど良くはないですが、技術的にはアクセス可能です。信頼できる MCP サーバーを実行する能力や、タスクを MCP にオフロードするサブエージェントなど、この方向への改善が多くあります。継続して取り組んでいます。

ありがとうございました。良い一日を。質問があれば今日・明日・金曜日もいますので気軽に声をかけてください。
